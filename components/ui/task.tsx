import Itask from '@/app/index';
import { useState } from 'react';

export interface ITask {
    title: string;
    category: string;
    isChecked: boolean;
  }
  
export default function Task({ text }: {task: TaskProps}) {
  return };


    const task: ITask = {
      title: 'My test item',
      category: 'Test category',
      isChecked: false,
    };
  


























}
    